using Unity.Burst.Intrinsics;
using UnityEngine;
using static UnityEngine.RuleTile.TilingRuleOutput;

public class Powers : MonoBehaviour
{

    //GameObject instance = Instantiate(Resources.Load("Fireball", typeof(GameObject))) as GameObject;
    //Instantiate(instance, aim.transform.position, aim.quaternionPosition );

}
